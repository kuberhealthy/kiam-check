---
apiVersion: kuberhealthy.github.io/v2
kind: HealthCheck
metadata:
  name: kiam
  namespace: kuberhealthy
spec:
  extraAnnotations:
    iam.amazonaws.com/role: <role-arn>
  runInterval: 5m
  timeout: 15m
  podSpec:
    spec:
      containers:
        - name: kiam
          image: ghcr.io/kuberhealthy/kiam-check:v0.1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: AWS_REGION
              value: us-west-2
      restartPolicy: Always
